# NMPC 阶段工作记录（当前版）

本文是当前有效结论的入口。逐次试验的原始记录保存在
[`docs/归档/阶段工作记录_完整历史_2026-08-29.md`](归档/阶段工作记录_完整历史_2026-08-29.md)，
但其中的中间参数、旧路径和旧版本结论不再直接作为当前操作依据。

## 1. 当前基线

| 项目 | 当前值/结论 |
|---|---|
| 控制后端 | ROS 2 + C++ 单进程 NMPC；Python 只负责启动、监督和测试编排 |
| Acados solver | 当前 hash `1c2d851e`，13 状态、4 输入、7 参数、30 步 |
| 时间配置 | MPC/轨迹 `sample_time=0.01 s`；控制/心跳 `control_period=0.01 s`、目标 100 Hz |
| 代价矩阵 | 原版 `Q=diag(70,70,140,70,70,70,91.91,91.91,91.91)`；`R=diag(4,91.91,91.91,91.91)` |
| 模型 | x500 总质量 `2.0643076923 kg`，`rate_tau=0.15 s` |
| ESO | 三轴速度 LESO，默认开启；默认带宽 `2.5 rad/s`、限幅 `1 m/s²`、启用延迟 `3 s`；三扰动候选带宽为 `2.5 rad/s` |
| 参考接口 | 上位机 `/nmpc/in/trajectory_setpoint` 完整 31 点 direct 轨迹 |
| RC 入口 | AUX6 高位才选择 RC-NMPC，低位继续使用外部轨迹 |
| 正式四项 | `hover`、`point_1m`、`circle`、`figure8` |

2026-09-04 修复了 C++ 默认参数与当前基线不一致的问题：节点内部的 `sample_time`、
ESO、热启动和启动控制状态兜底值，以及 C++ 仿真监督器的参考轨迹默认步长，均已同步为
`0.01 s`、ESO 开启（带宽 `2.5 rad/s`）、热启动、启动不输出控制。仅修改运行时兜底值，没有改变当前
`1c2d851e` solver 的 OCP；C++ 工作空间已重新编译，build/install 可执行文件校验和一致。

## 2. 最近有效四项回归

2026-09-03 使用当前 C++ 构建、无重心偏置、无风、位置/速度随机游走为 0，
四项均安全完成并正常降落解锁：

| 用例 | 位置 RMSE |
|---|---:|
| hover | `0.0414 m` |
| point_1m | `0.0593 m` |
| circle | `0.1439 m` |
| figure8 | `0.1736 m` |

四项位置 RMSE 总和为 `0.418188 m`，已保存为当前 `nominal_cpp` best：
`background/best_sitl_history/nominal_cpp/`。best 目录按模式只保留总误差最小的
一套完整记录，更新入口是 `integration/manage_best_sitl_history.py`。

同日另完成无扰动、ESO 关闭的 C++ 四项对照回归；该结果不覆盖 ESO 开启的
`nominal_cpp`，而是保存为独立的 `nominal_cpp_eso_off` best：
`background/best_sitl_history/nominal_cpp_eso_off/`。

| 用例 | 位置 RMSE | 最大误差 |
|---|---:|---:|
| hover | `0.0168 m` | `0.0265 m` |
| point_1m | `0.0715 m` | `0.1596 m` |
| circle | `0.1658 m` | `0.2690 m` |
| figure8 | `0.2124 m` | `0.3212 m` |

四项位置 RMSE 总和为 `0.466573 m`，均 `success=true`、正常降落解锁且无求解失败。
本轮结果目录为 `background/sitl_regression_cpp/disturbances_off_eso_off_20260903/`；
临时日志仍位于仓库外的 `/tmp/eso_nmpc_sitl_logs/`。

## 3. 最新三扰动 C++ 回归

2026-09-03 执行了 ESO 开启、重心前偏 `0.02 m`、X 向风 `0.5 m/s`、位置/速度随机游走
`0.002/0.005` 的完整四项回归。四项均安全降落，但均未通过跟踪阈值，不能作为基线或
best：

| 用例 | 结果 | 位置 RMSE | 最大误差 | Acados 求解失败 |
|---|---|---:|---:|---:|
| hover | FAIL，约 1.6 s 触发漂移保护 | `inf` | `inf` | 0 |
| point_1m | FAIL | `0.3865 m` | `1.4529 m` | 87 |
| circle | FAIL | `1.0934 m` | `2.0386 m` | 0 |
| figure8 | FAIL | `1.0898 m` | `2.0397 m` | 0 |

原始结果保存在 `background/sitl_regression_cpp/eso_three_disturbance_20260903/`，
汇总报告为 `suite_summary.md`，长图为 `trajectory_suite_long.png`，机器报告为
`background/json/cpp_regression_20260903_174852.json`。各项均记录
`landed_disarmed=true`；求解耗时中位数约 `3.09–3.14 ms`，P99 约 `4.52–4.68 ms`。
逐案例失败原因另记在 [`docs/四项回归失败记录.md`](四项回归失败记录.md)。

本轮同时修复了 `integration/mavlink_params.py` 对 MAVLink REAL32 参数的严格相等比较，
现在使用 `1e-6` 容差并增加了回归测试；此前 `0.002` 会被错误判定为写入失败。

## 4. ESO 带宽 `2.5 rad/s` 的三扰动 C++ 回归

2026-09-03 运行 ESO 开启、带宽覆盖为 `2.5 rad/s` 的三扰动完整四项回归。该轮使用当时
的整套共用服务流程，尚未按后来确定的“每个案例独立重启 PX4/Gazebo/Agent”规则复核。
扰动条件为：
重心前偏 `0.02 m`、X 向风 `0.5 m/s`、位置/速度随机游走扩散 `0.002/0.005`；四项均
安全完成并正常降落解锁，无 Acados 求解失败。

| 用例 | 位置 RMSE | 最大误差 |
|---|---:|---:|
| hover | `0.0348 m` | `0.0400 m` |
| point_1m | `0.0599 m` | `0.1756 m` |
| circle | `0.1443 m` | `0.2767 m` |
| figure8 | `0.1772 m` | `0.3189 m` |

四项位置 RMSE 总和为 `0.416207 m`，已保存为独立 best：
`background/best_sitl_history/three_disturbance_eso_on_bw2p5/`。该目录包含
`best_record.json`、`cpp_params.yaml`、四项 CSV/图和长图；其中 `best_record.json` 记录
扰动参数、ESO 带宽、控制周期、时域、solver hash、节点路径和源结果目录，可回溯本轮配置。
源结果为 `background/sitl_regression_cpp/three_disturbance_eso_on_bw2p5_20260903/`，
JSON 为 `background/json/cpp_regression_20260903_182239.json`。

本轮与此前 `3.0 rad/s` 三扰动失败轮次对比后，形成以下工作假设：ESO 带宽过大时，
观测器可能过快追踪模型残差、测量噪声和时序误差，并将其作为扰动反馈给逆动力学前馈
和 NMPC 预测，造成过补偿或闭环发散。带宽从 `3.0` 降至 `2.5 rad/s` 后本轮四项通过，
因此 `2.5 rad/s` 暂作为三扰动候选，不立即改写默认 YAML 基线；单次回归不能证明它
是最优值。后续应在固定三扰动条件下扫描 `1.5/2.0/2.5/3.0 rad/s`，并同时检查
`d_hat` 限幅、ESO 启用前后误差、求解失败次数和求解 P99。

## 5. AUX6 RC-NMPC 功能验证

2026-09-03 在无风、无重心偏置的 nominal SITL 中，使用 AUX6 完成了 C++ RC-NMPC
接管测试。注入序列为：起飞后 AUX6 拉高，前推 3 s，再松杆；四项外部轨迹回归的
best 不受本测试影响。

| 测试 | 结果 | RC 活跃样本 | 求解失败 | 说明 |
|---|---|---:|---:|---|
| AUX6 正常接管/前推/松杆 | PASS | `898` | `0` | 正常降落解锁 |
| AUX6 注入在 3 s 后停止 | 功能 PASS，安全保护未通过 | `899` | `0` | 旧逻辑仅保持当前 NMPC 参考，不能作为失效保护结论 |

两轮均记录了 `RC-NMPC reference enabled (AUX6)`。第二轮旧实现记录了
`RC input timeout; holding current NMPC reference`，但这只能证明没有继续积分上一条非零杆量，
不能证明已经安全退出 NMPC。RC 测试结果分别为
`background/sitl_regression_cpp/rc_aux6_hover_20260903/` 和
`background/sitl_regression_cpp/rc_aux6_timeout_20260903/`，机器报告分别为
`background/json/cpp_rc_injection_20260903_184723.json` 和
`background/json/cpp_rc_injection_20260903_184910.json`。

本轮同时修复了 RC 注入器始终只拉高 AUX1 的问题；现在注入器的 `--aux-channel` 与
C++ 节点的 `rc_aux_channel` 一致，默认值为 AUX6。临时日志和 ULog 位于
`/tmp/eso_nmpc_sitl_logs/rc_aux6_*/`，不进入结果目录。

## 6. 已解决并停止重复排查的问题

- 旧 C++ 本地构建和旧 solver hash 残留已移出工作区；正式回归默认使用
  `/home/cy/px4_ros2_ws/install/eso_nmpc_node/` 的当前 C++ 可执行文件。
- `sample_time` 已统一为 `0.02 s`；改变 OCP/离散化参数时必须重新生成 solver、
  编译生成文件并重建 C++ 节点。
- direct 完整轨迹已作为唯一正式参考接口；旧 PX4 平滑中转链路只作为历史对照。
- 长图改为读取监督器的 `trajectory.csv`；速度参考线优先使用监督器直接记录的
  `reference_velocity_*`，旧 CSV 才使用参考位置求导。
- PX4/Gazebo/Agent 的重复会话和时序问题已有启动检查；正式回归使用新实例和
  单一 DDS Agent。
- 非 best 的重复 `.ulg/.log` 已移入系统回收站；当前 best 原始记录保留。

## 7. 历史试验如何看待

| 历史内容 | 当前处理 |
|---|---|
| 早期 Q/R、R、位置/速度权重逐档扫描 | 只保留最终配置和结论；中间轮次不用于当前比较 |
| ESO 带宽、延迟和噪声扫描 | 作为扰动研究历史；默认 YAML 为 `2.5 rad/s`，必须通过固定扰动对比后再定型 |
| 时间同步、热启动、数组化和计时拆分 | 保留最终修复结论；重复测量不作为新的基线 |
| Python 四项回归 | 仅作为 C++ 迁移前历史基线，不与 C++ 数值横向混合 |
| 半速 SITL 和启动失败批次 | 仅作为异常样本；不能与正式 1.0 倍速四项结果比较 |
| ROS 1 迁移分支 | 当前决定继续 ROS 2，暂不作为主线工作；相关记录仅归档 |

## 8. 尚未完成的工作

1. 补齐 C++ 连续三次 Acados 失败锁存、停止 NMPC 输出、请求
   `AUTO.LOITER`、后台恢复求解和人工重新接管状态机。
2. RC 超时失效保护代码已修改为锁存关闭 NMPC 并通知外部管理器请求 AUTO.LOITER/Position；需重建 C++ 节点，
   在 SITL 中确认 `/nmpc/rc_timeout`、Position 模式和停止输出，再在真实遥控器链路上复测。
   2026-09-03 首次重跑未触发超时，是因为 PX4 原生 `ManualControlSetpoint` 覆盖了停止注入；
   已将注入器改为发送 AUX 保持高位的显式无效帧，避免测试假阴性。
   最终按完整重启规范验证：`rc_timeout_seen=true`、`rc_timeout_fallback_seen=true`、
   `rc_timeout_position_seen=true`、求解失败 `0`，证明 C++ 已停止输出且 PX4 已进入
   Position/AUTO.LOITER。整轮报告因当前 SITL 姿态失败导致监督器降落收尾超时而为 FAIL，
   该失败不覆盖核心 RC 超时保护结论；结果为
   `background/sitl_regression_cpp/rc_aux6_timeout_final_20260903/`，临时日志为
   `/tmp/eso_nmpc_sitl_logs/rc_aux6_timeout_final_20260903/`。
3. 在固定四项基线通过后，再对重心偏置、风和随机游走按模式建立 best 记录；ESO
   开启和关闭必须使用独立模式目录。
4. 完成 Orin ARM64 原生 solver、C++ 节点和实际 DDS 链路验证后，才进入真机流程。

## 2026-09-03 逐案例完整重启四项验证

为核对四项测试规范，按 `hover → point_1m → circle → figure8` 逐项执行 nominal、ESO
开启（默认带宽 `3.0 rad/s`）、无重心偏置/无风/无随机游走的 C++ 测试。每个案例均独立启动
`MicroXRCEAgent → Gazebo → PX4 → C++ NMPC`，结束后按 `C++节点 → PX4 → Gazebo → Agent`
关闭，并确认下一项开始前没有残留进程和端口。四项没有共享外部仿真服务。

| 用例 | 结果 | 失败原因 | 安全降落解锁 |
|---|---|---|---:|
| `hover` | FAIL | 约 2 s 触发安全边界，`drift=2.01 m`、`z=-0.12 m` | 是 |
| `point_1m` | FAIL | 约 2 s 触发安全边界，`drift=2.00 m`、`z=-0.22 m` | 是 |
| `circle` | FAIL | 轨迹完成，但位置 RMSE `1.1275 m`、最大误差 `2.0263 m` | 是 |
| `figure8` | FAIL | 轨迹完成，但位置 RMSE `2.2538 m`、最大误差 `4.6854 m` | 是 |

本次验证证明“逐项启动、逐项关闭”的生命周期可以执行，但四项性能验收未通过，因此不
更新 best。结果目录分别为
`background/sitl_regression_cpp/per_case_restart_20260903_200042/`（hover）和
`background/sitl_regression_cpp/per_case_restart_20260903_200213/`（其余三项）；临时日志
位于 `/tmp/eso_nmpc_sitl_logs/` 对应目录。失败原因已追加到
`docs/四项回归失败记录.md`。

## 2026-09-03 2.5 rad/s 三扰动逐案例重启复测

使用与历史候选 best 相同的条件重新验证：ESO 开启、带宽 `2.5 rad/s`，重心偏置
`0.02 m`、X 向风 `0.5 m/s`、位置/速度随机游走 `0.002/0.005`。本次严格按逐案例完整
重启规范执行，四项均独立启动和关闭外部服务，但未通过：

| 用例 | 结果 | 失败原因 | 安全降落解锁 |
|---|---|---|---:|
| `hover` | FAIL | 约 2.8 s 触发安全边界，`drift=2.02 m`、`z=0.38 m` | 是 |
| `point_1m` | FAIL | 约 1.7 s 触发安全边界，`drift=2.01 m`、`z=-0.13 m` | 是 |
| `circle` | FAIL | 轨迹完成，但位置 RMSE `1.3030 m`、最大误差 `2.0368 m` | 是 |
| `figure8` | FAIL | 轨迹完成，但位置 RMSE `1.1421 m`、最大误差 `2.0086 m` | 是 |

四项求解失败计数为 `0`，但跟踪和安全边界未通过，因此没有把 `2.5 rad/s` 写入默认
配置，也没有更新 best。结果目录为
`background/sitl_regression_cpp/three_disturbance_eso_on_bw2p5_per_case_20260903_201227/`，
临时日志位于 `/tmp/eso_nmpc_sitl_logs/three_disturbance_eso_on_bw2p5_per_case_20260903_201227/`。
与历史通过轮次相比，当前监督器的参考采样步长已从历史代码的 `0.02 s` 改为 `0.01 s`，
该差异尚未单独隔离，不能仅凭本轮判断 `2.5 rad/s` 本身失效。

随后仅将监督器参考步长改为 `0.02 s` 做隔离测试；当前 C++ 节点连续记录
`rejecting trajectory: points=31 sample_time=0.020000`，因为 C++ 节点和 solver 仍为
`0.01 s`，所以该轮没有产生有效 NMPC 控制数据，不能作为性能结论。若要正式测试
`0.02 s`，必须同步修改 OCP、重新生成 solver、重建 C++ 节点后再测试。

随后增加了默认开启的 `enforce_reference_sample_time` 参数，并用
`--reference-sample-time 0.02 --skip-reference-sample-time-check` 做了单项 hover 诊断。
本轮节点确认关闭了参考周期门限，产生 `351` 次成功求解，但仍使用 `0.01 s` solver；飞机
约 `0.86 s` 后触发安全边界，水平漂移约 `2.02 m`、高度 `z=-0.38 m`，随后安全降落并解锁。
因此结论是：监督器 `0.02 s` 不能只通过跳过门限来使用，当前失配配置不可用；本轮不计入
四项回归或 best。结果目录为
`background/sitl_regression_cpp/diagnostic_refdt002_skipcheck_20260903_retry/`，JSON 为
`background/json/cpp_regression_20260903_203715.json`，临时日志位于
`/tmp/eso_nmpc_sitl_logs/diagnostic_refdt002_skipcheck_20260903_retry/`。正式使用 `0.02 s`
仍需同步 OCP、生成 solver、重建 C++ 节点并重新按四项完整重启规范验证。

## 2026-09-03 旧归档可执行文件隔离对照

为检查历史成功轮次是否因可执行文件被覆盖而无法复现，使用现存归档的旧 C++ 节点
`e2d8d978` 与对应 solver，单独启动全新的 Agent/Gazebo/PX4，只运行 `hover`；监督器参考
步长固定为 `0.02 s`。该版本与历史成功报告中的 `1c2d851e` solver 并不相同，因此仅作为
旧版本对照，不作为历史成功版本恢复结论。

本轮旧节点启动信息为 `dt=0.020`，求解成功 `1124` 次、失败 `0` 次，能够完成起飞和安全降落；
但位置 RMSE 为 `0.5388 m`、最大误差为 `0.9136 m`，仍未达到正式验收阈值。相同无扰动 hover
下，当前 `1c2d851e/0.01 s` 节点在诊断放宽参考周期后约 `0.86 s` 触发漂移保护，说明可执行文件
和 solver 版本确实会影响行为，但旧归档版也不能解释或复现之前的四项成功结果。

旧版结果目录为
`background/sitl_regression_cpp/historical_candidate_e2d8d978_refdt002_20260903/`，JSON 为
`background/json/cpp_regression_20260903_204644.json`，旧节点路径为
`/home/cy/eso_nmpc_archive/20260903_old_artifacts/local_builds/build_cpp_e2d8d978/eso_nmpc_node/eso_nmpc_node`。
本轮不更新 best；仿真服务已关闭。

## 2026-09-03 无 ESO、无扰动四项回归复测

使用当前实际运行节点 `1c2d851e`、`0.01 s`、参考周期门限开启、ESO 关闭，并将 Gazebo
重心偏置和风临时置零、PX4 位置/速度随机游走临时设为 `0/0`。四个案例均按完整重启流程
逐项执行，均正常降落解锁，但只有 hover 达到当前验收阈值：

| 用例 | 结果 | 位置 RMSE | 最大误差 | 求解失败 |
|---|---|---:|---:|---:|
| `hover` | PASS | `0.2074 m` | `0.2614 m` | 0 |
| `point_1m` | FAIL | `0.3050 m` | `0.4232 m` | 0 |
| `circle` | FAIL | `0.3417 m` | `0.5279 m` | 0 |
| `figure8` | FAIL | `0.2996 m` | `0.5520 m` | 0 |

本轮四项不更新 `nominal_cpp_eso_off` best；此前保存的 `0.466573 m` best 不变。报告记录的
总 RMSE 为 `1.1537 m`，节点 SHA256 为
`c71f818655f0c8f75cfc906ba9f7a0d5571916c58b6f297f47f30889637da0a6`。结果目录为
`background/sitl_regression_cpp/no_eso_no_disturbance_per_case_20260903_205904/`，完整汇总
JSON 为 `background/json/cpp_regression_20260903_210332_full.json`，四项长图为该结果目录下的
`trajectory_suite_long.png`。circle 参数恢复曾出现一次 `SIM_GZ_ODOM_RW_V` 警告，但 figure8
启动时已重新确认并设置为 `0`，测试结束后仿真模型和世界文件均恢复原配置，所有服务进程已关闭。

## 2026-09-03 无 ESO、无扰动四项回归复测（第二轮）

在当前已重新编译的 C++ 节点（solver `1c2d851e`、节点 SHA256
`c71f818655f0c8f75cfc906ba9f7a0d5571916c58b6f297f47f30889637da0a6`）上，按每个案例完整
重启 Agent/Gazebo/PX4/节点的规范重新运行。ESO 关闭，Gazebo 重心偏置和风临时置零，PX4
位置/速度随机游走为 `0/0`，参考采样时间检查开启，控制周期为 `0.01 s`。

| 用例 | 结果 | 位置 RMSE | 最大误差 | 求解失败 | ACADOS median/p95/max (ms) |
|---|---|---:|---:|---:|---:|
| `hover` | PASS | `0.2169 m` | `0.2412 m` | 0 | `3.027/5.037/8.608` |
| `point_1m` | FAIL | `0.3143 m` | `0.4728 m` | 0 | `2.969/4.608/8.270` |
| `circle` | FAIL | `0.3169 m` | `0.4839 m` | 0 | `3.002/4.609/8.076` |
| `figure8` | FAIL | `0.3441 m` | `0.5915 m` | 0 | `2.944/4.463/8.553` |

四项均正常完成流程并 `landed_disarmed=true`，未观察到 RC/Offboard 超时，ACADOS 求解失败
为 `0`。本轮仍未全部达到跟踪验收阈值，因此不更新 `nominal_cpp_eso_off` best；结果目录为
`background/sitl_regression_cpp/no_eso_no_disturbance_per_case_20260903_212133/`，完整汇总
JSON 为 `background/json/cpp_regression_20260903_212604_full.json`，四项长图为该结果目录下的
`trajectory_suite_long.png`。测试结束后已恢复 Gazebo 原始重心偏置 `0.02 m` 和世界风速
`0.5 m/s`，所有仿真服务进程已关闭。

## 2026-09-03 误将控制周期改为 0.02 s 的无 ESO、无扰动回归（作废）

本轮曾错误地将控制/心跳周期也设为 `0.02 s`。MPC/轨迹采样步长为 `0.02 s`，重新生成 solver
`e2d8d978` 并重建 C++ 节点；该控制周期配置不符合当前 100 Hz 规范，因此本轮结果仅作
诊断记录。ESO 默认关闭；Gazebo
重心偏置和风临时置零，PX4 位置/速度随机游走为 `0/0`。先按同一完整重启生命周期通过
hover 冒烟，再按每个案例独立重启 Agent/Gazebo/PX4/节点完成四项回归。

| 用例 | 结果 | 位置 RMSE | 最大误差 | 求解失败 | ACADOS median/p95/max (ms) |
|---|---|---:|---:|---:|---:|
| `hover` | PASS | `0.1355 m` | `0.1557 m` | 0 | `2.936/3.773/7.674` |
| `point_1m` | PASS | `0.1967 m` | `0.2888 m` | 0 | `2.965/3.863/9.694` |
| `circle` | PASS | `0.2230 m` | `0.3302 m` | 0 | `2.975/3.835/8.570` |
| `figure8` | FAIL | `0.2681 m` | `0.4677 m` | 0 | `2.881/3.657/8.764` |

四项均安全落地并解除锁定，前三项达到跟踪阈值，figure8 仅因误差超过阈值失败；本轮不
更新 best，也不作为当前 100 Hz 控制周期结论。完整汇总 JSON 为
`background/json/cpp_regression_20260903_214146_full.json`，四项长图为结果目录
`background/sitl_regression_cpp/period_002_no_eso_no_disturbance_per_case_20260903_214146/`
下的 `trajectory_suite_long.png`。配置已恢复原始 Gazebo 重心偏置和风速，所有服务进程已
关闭；figure8 失败原因已同步写入 `docs/四项回归失败记录.md`。

## 2026-09-03 MPC/轨迹 0.02 s、控制 100 Hz 的无 ESO、无扰动四项回归

根据时间基准纠正：MPC 预测离散和轨迹器采样使用 `0.02 s`，C++ 控制/心跳周期使用
`0.01 s`（100 Hz），参考周期门限开启。solver 为重新生成的 `e2d8d978`，当前 C++ 节点
SHA256 为 `37faaab39c17e6b44d76dde2ff85e081a966872f4f873d982542f2bad1ec9e3c`。ESO 关闭，
重心偏置、风和位置/速度随机游走均为 0；四项按每个案例独立重启 Agent/Gazebo/PX4/节点。

| 用例 | 结果 | 位置 RMSE | 最大误差 | 求解失败 | ACADOS median/p95/max (ms) |
|---|---|---:|---:|---:|---:|
| `hover` | PASS | `0.1487 m` | `0.1626 m` | 0 | `3.104/4.528/9.830` |
| `point_1m` | PASS | `0.2050 m` | `0.3275 m` | 0 | `3.092/4.556/9.343` |
| `circle` | FAIL | `0.3616 m` | `0.7699 m` | 0 | `3.121/4.466/7.282` |
| `figure8` | FAIL | `0.2791 m` | `0.4727 m` | 0 | `3.070/4.233/9.483` |

四项均安全落地并解除锁定，hover 和 point_1m 通过；circle、figure8 的失败原因为
`trajectory completed`（轨迹完成但跟踪误差超过阈值），不是求解失败或飞行安全故障。
本轮不更新 best。完整汇总 JSON 为
`background/json/cpp_regression_20260903_215459_full.json`，四项长图为结果目录
`background/sitl_regression_cpp/period_002_mpc_100hz_no_eso_no_disturbance_per_case_20260903_215459/`
下的 `trajectory_suite_long.png`。Gazebo 配置已恢复，所有服务进程已关闭。

## 2026-09-03 不使用热启动的无 ESO、无扰动四项回归

保持 MPC/轨迹 `0.02 s`、控制/心跳 `0.01 s`（100 Hz），通过回归参数
`--disable-warm-start` 关闭 C++ Acados 的状态、控制量和乘子热启动；ESO 关闭，重心偏置、
风和位置/速度随机游走均为 0。四项按独立重启规范执行。

| 用例 | 结果 | 位置 RMSE | 最大误差 | 求解失败 | ACADOS median/p95/max (ms) |
|---|---|---:|---:|---:|---:|
| `hover` | PASS | `0.1827 m` | `0.2028 m` | 0 | `3.080/4.526/7.963` |
| `point_1m` | PASS | `0.1811 m` | `0.2891 m` | 0 | `3.177/4.500/14.049` |
| `circle` | FAIL | `0.3399 m` | `0.6779 m` | 0 | `3.091/4.333/6.754` |
| `figure8` | FAIL | `0.4101 m` | `0.8268 m` | 0 | `3.019/4.192/12.897` |

四项均安全落地并解除锁定，失败项均为 `trajectory completed` 但跟踪误差超阈值；不更新
best。完整汇总 JSON 为
`background/json/cpp_regression_20260903_220257_full.json`，四项长图为结果目录
`background/sitl_regression_cpp/period_002_mpc_100hz_no_warm_no_eso_no_disturbance_per_case_20260903_220257/`
下的 `trajectory_suite_long.png`。节点日志确认 `mpc_dt=0.020`、`control_period=0.010`，
仿真配置已恢复，所有服务进程已关闭。

## 2026-09-03 默认 ESO 带宽 2.5 rad/s、无扰动四项回归

在正确时间配置下开启 ESO：MPC/轨迹采样步长 `0.02 s`，C++ 控制/心跳周期 `0.01 s`
（100 Hz），solver `e2d8d978`，warm start 开启。ESO 显式设置为默认带宽 `2.5 rad/s`；
重心偏置、风和位置/速度随机游走均为 0，四项按独立重启规范执行。

| 用例 | 结果 | 位置 RMSE | 最大误差 | 求解失败 | ACADOS median/p95/max (ms) |
|---|---|---:|---:|---:|---:|
| `hover` | PASS | `0.0706 m` | `0.1166 m` | 0 | `3.052/4.013/5.741` |
| `point_1m` | PASS | `0.1138 m` | `0.4913 m` | 0 | `3.170/4.212/9.442` |
| `circle` | FAIL | `0.3214 m` | `0.8338 m` | 0 | `3.073/4.053/8.606` |
| `figure8` | FAIL | `0.3280 m` | `0.6279 m` | 0 | `3.016/3.957/6.063` |

四项均安全落地并解除锁定，失败项均为轨迹完成但跟踪误差超过阈值；本轮不更新 best。
完整汇总 JSON 为
`background/json/cpp_regression_20260903_221322_full.json`，四项长图为结果目录
`background/sitl_regression_cpp/period_002_mpc_100hz_eso_on_bw2p5_no_disturbance_per_case_20260903_221322/`
下的 `trajectory_suite_long.png`。节点日志确认 `mpc_dt=0.020`、`control_period=0.010`，
solver 已编译并链接 `e2d8d978`；测试结束后已恢复 Gazebo 配置，所有服务进程已关闭。

## 2026-09-03 三扰动 ESO 带宽扫参复核

针对“ESO 应在扰动开启时调参”的要求，使用当前 C++ 节点、solver `e2d8d978`，在
重心前偏 `0.02 m`、X 向风 `0.5 m/s`、位置/速度随机游走 `0.002/0.005`、seed `42`
下，分别测试 ESO `1.5/2.0/2.5/3.0 rad/s`。控制/心跳为 `0.01 s`（100 Hz），
MPC/轨迹步长为 `0.02 s`，热启动开启；每个带宽的每个案例均独立重启
`MicroXRCEAgent → Gazebo → PX4 → C++ NMPC`。

| ESO 带宽 | 四项通过 | 安全降落解锁 | 位置 RMSE 总和 | 求解失败 |
|---:|:---:|:---:|---:|---:|
| `1.5 rad/s` | 否 | 是 | `1.6954 m` | 0 |
| `2.0 rad/s` | 否 | 是 | `1.4418 m` | 0 |
| `2.5 rad/s` | 否 | 是 | `1.5809 m` | 0 |
| `3.0 rad/s` | 否 | 是 | `1.5965 m` | 0 |

本轮没有发现可直接作为四项 best 的 ESO 带宽；失败均来自跟踪误差超限，不是 Acados
求解崩溃。当前不能仅凭无扰动结果把 `2.5 rad/s` 认定为三扰动最优，也不更新三扰动
best 或默认参数。低带宽 `1.5/2.0` 的悬停 RMSE 已约 `0.57 m`，`2.5/3.0` 也约
`0.60/0.56 m`，说明当前异常不呈现简单的“带宽越大越发散”规律，应优先排查当前
C++ 扰动闭环与历史成功轮次的实现/参数差异。

复核历史 `three_disturbance_eso_on_bw2p5` best 后确认，它实际使用的参数快照仍是
`sample_time=0.01 s`、solver `1c2d851e`，与当前已明确的 MPC/轨迹 `0.02 s`、solver
`e2d8d978` 不同；因此历史通过结果不能直接证明当前时序下某个 ESO 带宽可用。

完整汇总为 `background/json/cpp_eso_bandwidth_sweep_three_disturbance_20260903_223146.json`，
原始结果为 `background/sitl_regression_cpp/eso_bandwidth_sweep_three_disturbance_20260903_223146/`；
每个案例的失败原因已追加到 `docs/四项回归失败记录.md`。四项结束后已恢复 Gazebo
模型与世界文件，确认没有残留 PX4、Gazebo、Agent 或 NMPC 进程。

## 2026-09-03 三扰动 ESO 关闭对照

为判断失败是否由 ESO 带宽造成，在相同三扰动、时序、solver、热启动和逐案例重启条件下
关闭 ESO 重新运行四项。结果为：`hover 0.6021 m`、`point_1m 0.2007 m`、
`circle 0.4028 m`、`figure8 0.4536 m`；四项均 `success=false`，但均安全落地解锁，
Acados 求解失败为 `0`。

这组对照说明当前三扰动失败并非简单由 ESO 带宽选择造成，因此不把 `2.0 rad/s` 的较小
RMSE 总和误写成可用 best，也不修改当前默认 ESO 配置。下一步应对照历史成功轮次检查
三扰动实际施加方式、C++ 扰动估计/补偿链路和模型参数，而不是继续盲目扩大带宽扫描范围。
对照 JSON 为 `background/json/cpp_three_disturbance_eso_off_reference_20260903_225136.json`，
四项长图为 `background/sitl_regression_cpp/three_disturbance_eso_off_reference_20260903_225136/trajectory_suite_long.png`。

## 2026-09-04 MPC 预测步长扫参：ESO 2.5 + 三项扰动

为验证预测/轨迹离散步长对当前 C++ 闭环的影响，固定 ESO 带宽 `2.5 rad/s`、三项扰动
（重心前偏 `0.02 m`、X 向风 `0.5 m/s`、位置/速度随机游走 `0.002/0.005`、seed `42`），
控制/心跳仍为 `0.01 s`（100 Hz），对 MPC/轨迹步长 `0.02–0.10 s` 按 `0.01 s` 间隔逐档测试。
每一档都重新生成 Acados solver、重新编译 C++ 节点，并记录 solver hash 和可执行文件 SHA256；
四个案例按独立重启规范逐项执行。

| MPC/轨迹步长 | solver | 四项全通过 | 安全收尾 | 求解失败总数 | 位置 RMSE 总和 |
|---:|---|:---:|:---:|---:|---:|
| `0.02 s` | `e2d8d978` | 否 | 是 | `0` | `1.6013 m` |
| `0.03 s` | `1a3ce384` | 否 | 是 | `0` | `1.0915 m` |
| `0.04 s` | `55c75aa4` | 否 | 是 | `162` | `inf` |
| `0.05 s` | `c3265386` | 否 | 是 | `698` | `4.5553 m` |
| `0.06 s` | `8fbe1080` | 否 | 是 | `343` | `2.9387 m` |
| `0.07 s` | `2a8efd88` | 否 | 是 | `265` | `1.3592 m` |
| `0.08 s` | `d7b21d55` | 否 | 是 | `160` | `inf` |
| `0.09 s` | `9a40794c` | 否 | 否 | `547` | `inf` |
| `0.10 s` | `10a247fb` | 否 | 是 | `236` | `inf` |

严格按“四项跟踪通过、全程安全、正常落地解锁、求解失败为 0”判定，本轮没有可用步长，
不更新任何 best，也不修改默认配置。`0.04 s` 仅 hover/point_1m 通过，但 circle 出现
`162` 次求解失败并触发安全边界；`0.07 s` 的 circle 虽通过指标，四项仍有求解失败和
其他案例越界；`0.09/0.10 s` 已出现明显发散或大量求解失败。因此当前基线继续使用
MPC/轨迹 `0.02 s`、控制 `0.01 s`、solver `e2d8d978`。

汇总报告为
`background/sitl_regression_cpp/prediction_step_sweep_eso2p5_three_disturbance_20260903_232249.md`，
机器结果为
`background/json/cpp_prediction_step_sweep_eso2p5_three_disturbance_20260903_232249.json`，
各步长目录同时保留对应的 C++ 可执行文件、参数快照、四项 CSV 和长图；临时 SITL 日志位于
`/tmp/eso_nmpc_sitl_logs/prediction_step_sweep_eso2p5_three_disturbance_20260903_232249/`。
扫参完成后已恢复外部仿真扰动文件、跟踪配置和已安装的 `e2d8d978` C++ 节点，确认没有残留
仿真或控制进程。

## 2026-09-04 无扰动 ESO 开关四项对照

在当前 `sample_time=0.02 s`、控制周期 `0.01 s`、solver `e2d8d978` 下，将 Gazebo
重心偏置和风、PX4 位置/速度随机游走全部置零，分别执行 ESO `2.5 rad/s` 开启和关闭
两套四项 C++ 回归。每个案例均独立重启 Agent、PX4、Gazebo 和 C++ 节点；结果不更新 best。

| 模式 | hover | point_1m | circle | figure8 | RMSE 总和 |
|---|---:|---:|---:|---:|---:|
| ESO 开启 `2.5 rad/s` | PASS `0.0415 m` | PASS `0.1262 m` | FAIL `0.5633 m` | FAIL `0.5176 m` | `1.2487 m` |
| ESO 关闭 | PASS `0.0303 m` | PASS `0.1274 m` | PASS `0.1394 m` | FAIL `0.6375 m` | `0.9346 m` |

两套四项均正常落地解锁且 Acados 求解失败为 `0`；失败项均为轨迹完成但跟踪误差超阈值。
本轮说明在无扰动条件下，ESO 开启并未改善当前 circle/figure8 的跟踪，ESO 关闭的总误差
更小，但仍未满足四项全部通过，因而不修改默认参数或 best。完整机器汇总为
`background/json/cpp_no_disturbance_eso_comparison_20260904_003800_summary.json`，
两套结果目录分别为
`background/sitl_regression_cpp/no_disturbance_eso_comparison_20260904_003800_eso_on_bw2p5/`
和
`background/sitl_regression_cpp/no_disturbance_eso_comparison_20260904_003800_eso_off/`；
四项长图已分别保存在两个结果目录下的 `trajectory_suite_long.png`。测试完成后已恢复
三项扰动仿真文件，当前基线不变。

## 2026-09-04 新热启动方案试验与回退

为验证此前记录中提出的时序改进方向，在不改变 solver、参数和控制周期的前提下，临时试验
了 C++ `time_aligned_delta` 热启动：按实际里程计间隔移动上一轮预测，并将上一轮控制量相对
前馈的修正量重新锚定到当前前馈。试验条件为 `sample_time=0.02 s`、控制周期 `0.01 s`、
ESO 关闭、三项扰动关闭；冒烟和四项回归均按每个案例独立重启 Agent、PX4、Gazebo 和 C++ 节点。

冒烟 `hover` 通过，求解失败为 `0`，RMSE `0.0484 m`。四项结果为：
`hover 0.0289 m`、`point_1m 0.0917 m`、`circle 0.2261 m` 通过，`figure8 0.5977 m`
未通过；四项均安全落地解锁，求解失败均为 `0`。由于正式四项未全部通过，按回退条件撤销
该热启动改动，没有写入默认参数，也没有更新 best。四项长图保留在
`background/sitl_regression_cpp/warm_start_time_aligned_20260904_full/trajectory_suite_long.png`，
对应各案例 JSON 为 `background/json/cpp_regression_20260904_005637.json`、
`cpp_regression_20260904_005721.json`、`cpp_regression_20260904_005856.json` 和
`cpp_regression_20260904_005951.json`。

回退后重新编译 `e2d8d978` C++ 节点，并以无 ESO、无扰动 `hover` 复核：通过，求解失败为 `0`，
安全落地解锁。当前源码、运行脚本和默认热启动均恢复为试验前版本；回退复核记录为
`background/json/cpp_regression_20260904_010157.json`。本次新方案结果仅用于诊断，不作为
控制基线。

## 2026-09-04 位置 Q 矩阵调整

针对当前无 ESO、无扰动四项中连续轨迹位置误差偏大的现象，仅提高位置状态代价，保持速度、
姿态、输入代价以及模型和时间配置不变。位置代价由 `diag(70,70,140)` 调整为
`diag(100,100,200)`，生成新的 solver `f9af744f`，并重新编译 C++ 节点；配置测试 `3/3`、
运行时测试 `6/6` 通过。

在无 ESO、无扰动且每项独立重启的四项回归中，结果为：
`hover 0.0210 m`、`point_1m 0.0822 m`、`circle 0.1467 m`、`figure8 0.5366 m`。
四项均安全落地解锁，求解失败为 `0`，求解时间 P99 约 `4.24–4.47 ms`；figure8 仍未达到
当前跟踪阈值，但相对调整前的 `0.6375 m` 有改善，四项总 RMSE 由 `0.9346 m` 降至约
`0.7864 m`。本档 Q 保留为当前调参候选，不更新四项 best；长图为
`background/sitl_regression_cpp/q_position100_20260904_full/trajectory_suite_long.png`，
机器报告为各案例对应的 `background/json/cpp_regression_20260904_011027.json`、
`cpp_regression_20260904_011110.json`、`cpp_regression_20260904_011245.json` 和
`cpp_regression_20260904_011341.json`。

## 2026-09-04 原版 Q、sample 0.01 s 无 ESO 无扰动基线候选

为后续热启动、Q 矩阵和 ESO 修改建立可回溯对比点，将配置固定为：控制 100 Hz，
MPC/轨迹 `sample_time=0.01 s`，原版位置代价 `diag(70,70,140)`，ESO 关闭；Gazebo
风速、重心偏置以及 PX4 位置/速度随机游走均为 0。由于 `sample_time` 和 Q 改变，已重新生成
solver `1c2d851e` 并重建 C++ 节点，实际安装可执行文件也已核对为该 solver。

四项按独立重启规范完成，结果如下：

| 用例 | 结果 | Position RMSE | 最大误差 | Acados 求解失败 | 原因 |
|---|---|---:|---:|---:|---|
| hover | PASS | `0.1169 m` | `0.1720 m` | 0 | trajectory completed |
| point_1m | FAIL | `0.1767 m` | `2.0283 m` | 14 | 漂移安全边界 `2.02 m`、`z=-0.92` |
| circle | FAIL | `0.1639 m` | `2.3144 m` | 0 | 最大误差超过 `0.6 m` |
| figure8 | FAIL | `0.3417 m` | `3.9024 m` | 4 | 最大误差超过 `0.6 m` |

四项 Position RMSE 总和为 `0.799111 m`，但本轮不更新 `nominal_cpp_eso_off` best，
只作为后续参数实验的候选基线。完整汇总、长图和参数快照保存在：
`background/sitl_regression_cpp/baseline_no_eso_no_disturbance_100hz_sample001_q_original_20260904_075959/`；
机器汇总为该目录下的 `suite_summary.json`，长图为 `trajectory_suite_long.png`。四项对应报告为
`background/json/cpp_regression_20260904_080016.json`、
`cpp_regression_20260904_080103.json`、`cpp_regression_20260904_080224.json` 和
`cpp_regression_20260904_080326.json`。失败原因已追加到
`docs/四项回归失败记录.md`。

## 2026-09-04 轨迹发布断流修复与最终无 ESO 无扰动基线

此前多轮 point/连续轨迹失败经检查确认：Acados 求解本身通常只有约 `3–5 ms`，PX4 本地
位置流也没有持续中断；真正的问题是监督器在 ROS 回调路径中同步发布完整轨迹，DDS/RMW
短暂阻塞后造成约 `2–3 s` 的参考轨迹空档，进而使 C++ 控制输出和跟踪误差失真。另有一轮
诊断使用了 Python best-effort、C++ reliable 的不兼容轨迹 QoS，已在失败文档中单独标记，
不作为控制器结论。

本轮修复包括：监督器增加独立后台轨迹发布线程；C++ 轨迹订阅与 odometry/求解使用独立
callback group；C++ 本机 odometry 接收 watchdog 先于轨迹新鲜度判断；外层回归器持续读取
子进程输出但不再把每行 ROS 日志镜像到终端。Python 仍只负责启动、监督、参考轨迹和结果
整理，实际 NMPC 控制始终由持续运行的 C++ `eso_nmpc_node` 完成。

修复后以正式默认配置完成四项独立重启回归：sample/控制/参考轨迹均 `0.01 s`（100 Hz），
solver `1c2d851e`，冷启动，ESO 关闭，重心偏置/风/位置和速度随机游走均关闭。四项均通过：

| 用例 | Position RMSE | 最大误差 | Solve P99 | 轨迹发布最大间隔 |
|---|---:|---:|---:|---:|
| hover | `0.0320 m` | `0.0673 m` | `5.48 ms` | `12.8 ms` |
| point_1m | `0.0781 m` | `0.1897 m` | `5.33 ms` | `15.2 ms` |
| circle | `0.1239 m` | `0.2153 m` | `5.29 ms` | `14.2 ms` |
| figure8 | `0.2303 m` | `0.3275 m` | `5.45 ms` | `13.4 ms` |

四项位置 RMSE 总和为 `0.464315 m`，已覆盖更新
`background/best_sitl_history/nominal_cpp_eso_off/`。最终机器报告为
`background/json/cpp_regression_20260904_124513.json`，四项长图为
`background/sitl_regression_cpp/fixed_thread_nominal_20260904/trajectory_suite_long.png`，
参数快照、可执行文件 SHA-256 和四个单项来源报告均写入 best 的 `best_record.json`。

## 2026-09-04 已知线程竞态修复复测

针对 Python 监督器后台轨迹线程与 ROS 回调共享状态、退出时未 join，以及 C++ RC 轨迹构造
与 heartbeat watchdog 共享 RC 状态的问题，已完成加锁、线程退出同步和 C++ 重建。随后按
正式规范逐项重启运行无 ESO、无扰动、冷启动四项回归，四项均通过，未触发线程相关故障：

| 用例 | Position RMSE | 最大误差 | Acados 求解 P99 | rx→pub P99 | 求解失败 | 轨迹发布最大间隔 |
|---|---:|---:|---:|---:|---:|---:|
| hover | `0.060690 m` | `0.081287 m` | `4.385 ms` | `5.371 ms` | 0 | `12.77 ms` |
| point_1m | `0.077632 m` | `0.184546 m` | `4.849 ms` | `5.845 ms` | 0 | `15.00 ms` |
| circle | `0.150573 m` | `0.248941 m` | `4.558 ms` | `5.519 ms` | 0 | `12.30 ms` |
| figure8 | `0.192288 m` | `0.261711 m` | `4.411 ms` | `5.385 ms` | 0 | `12.04 ms` |

四项 Position RMSE 总和为 `0.481183 m`，高于当前 `0.464315 m` best，因此不覆盖
`background/best_sitl_history/nominal_cpp_eso_off/`。机器报告为
`background/json/cpp_regression_20260904_132851.json`，长图为
`background/sitl_regression_cpp/thread_safe_nominal_20260904_final3/trajectory_suite_long.png`。

## 2026-09-04 控制器状态竞态修复与 best 更新

复查线程模型后确认，RC timeout watchdog 仍可能在 Acados 求解期间清除 warm start。新增
`controller_mutex_`，统一串行化 warm-start 清除、Acados solve 和求解时间快照；同时把
enable/disable 状态转换放入 RC 状态锁，避免重新启用与 watchdog 交叉覆盖故障锁存。完成
C++ 重建后再次按四项独立重启规范运行，四项全部通过：

| 用例 | Position RMSE | 最大误差 | Solve P99 | rx→pub P99 | 求解失败 |
|---|---:|---:|---:|---:|---:|
| hover | `0.030918 m` | `0.049287 m` | `4.593 ms` | `5.542 ms` | 0 |
| point_1m | `0.069052 m` | `0.155490 m` | `4.512 ms` | `5.513 ms` | 0 |
| circle | `0.151447 m` | `0.265290 m` | `4.265 ms` | `5.212 ms` | 0 |
| figure8 | `0.184943 m` | `0.297353 m` | `4.391 ms` | `5.374 ms` | 0 |

四项 Position RMSE 总和为 `0.436360 m`，低于原 `0.464315 m`，已更新
`background/best_sitl_history/nominal_cpp_eso_off/`。机器报告为
`background/json/cpp_regression_20260904_133757.json`，长图为
`background/sitl_regression_cpp/thread_safe_nominal_20260904_final4/trajectory_suite_long.png`，
本轮的 C++ 节点 SHA-256 为
`ec2cfa933d85512dd6e0eedd8764b9a63ef98bd2414f88109ec0eb3ec5c199e4`。

## 2026-09-04 RC 逻辑修复与注入验证

修复 C++ RC 输入逻辑：要求 `data_source=SOURCE_RC` 和 `valid=true`；AUX6 高但没有有效
RC、输入无效或输入超时，直接触发统一 NMPC 失效保护；超时锁存同时清除 warm start，
监督器在飞行前收到该故障也不再忽略。RC 测试判定同步修复为不使用外部 hover RMSE，
避免正常摇杆运动被误判为跟踪失败。

| 场景 | 结果 | RC 有效样本 | 超时/Position fallback | 求解失败 | 安全落地解锁 |
|---|---:|---:|---:|---:|---:|
| AUX6 接管、前推、松杆保持 | `PASS` | 884 | 否/不适用 | 0 | 是 |
| `drop-after=3.0 s` | `PASS` | 200 | 是/是 | 0 | 是 |

结果目录为 `background/sitl_regression_cpp/rc_logic_fix_normal2_20260904/` 和
`rc_logic_fix_timeout2_20260904/`；机器报告保存在 `background/json/cpp_rc_injection_*.json`。

## 2026-09-04 逐步启用热启动与 ESO 的基线验证

按“先热启动、再 ESO”的顺序修改两份实际配置文件，并在每一步重新构建 C++ 节点；四项
测试均没有使用 `--enable-eso`、`--disable-eso` 或 `--disable-warm-start` 运行时覆盖。
控制/轨迹/参考步长为 `0.01 s`，solver 为 `1c2d851e`，扰动全部关闭，ESO 带宽从 YAML
读取为 `2.5 rad/s`。

第一步仅开启热启动、关闭 ESO，四项均通过：`hover 0.0267 m`、`point_1m 0.0686 m`、
`circle 0.1642 m`、`figure8 0.2080 m`，求解失败为 0。该轮报告为
`background/json/cpp_regression_20260904_150229.json`，结果目录为
`background/sitl_regression_cpp/warm_start_eso_off_20260904/`。

第二步在保持热启动的基础上开启 ESO。首次连续案例试验中，前三项通过，但第四项
`figure8` 因连续重启节点后的 DDS 会话未收到有效状态而超时，原因已单独记录在
[`docs/四项回归失败记录.md`](四项回归失败记录.md)，该轮没有进入 best。重启整套服务后
单独复测 `figure8` 通过，说明不是 ESO 求解发散。

随后发现前一轮构建命令更新了仓库内 `install/`，而回归默认调用的是
`/home/cy/px4_ros2_ws/install/`；已改为在实际 PX4 ROS2 工作空间重新编译，并核对 build/install
可执行文件一致，当前 SHA-256 为
`fc42c5c2065fda60fefd5075c43ab845ba323677b9e24e0708cee061a55069d7`。

使用这份新可执行文件，按每项独立启动和关闭全新 PX4、Gazebo、MicroXRCEAgent 的正式流程
重跑四项，结果如下：

| 用例 | 结果 | Position RMSE | 最大误差 | rx→pub P99 | Solve P99 | 求解失败 |
|---|---|---:|---:|---:|---:|---:|
| hover | PASS | `0.0351 m` | `0.0572 m` | `6.91 ms` | `5.88 ms` | 0 |
| point_1m | PASS | `0.0559 m` | `0.1435 m` | `6.17 ms` | `5.14 ms` | 0 |
| circle | PASS | `0.1376 m` | `0.2054 m` | `6.22 ms` | `5.19 ms` | 0 |
| figure8 | PASS | `0.1681 m` | `0.2625 m` | `6.45 ms` | `5.45 ms` | 0 |

四项 Position RMSE 总和为 `0.396686 m`，四项均 `landed_disarmed=true`，已更新
`background/best_sitl_history/nominal_cpp_warm_start_eso_on_bw2p5/`。正式汇总报告为
`background/json/cpp_regression_20260904_153452_formal.json`，四项长图为
`background/sitl_regression_cpp/incremental_warm_start_eso_on_formal2_20260904/formal_suite/trajectory_suite_long.png`。
best 内含参数快照、四项 CSV/单项图、节点校验和、solver hash 和源报告，可完整回溯本次实际配置。

因此当前仓库默认基线为：C++ 节点、`sample_time/control_period=0.01 s`、热启动开启、ESO
开启、ESO 带宽 `2.5 rad/s`、无扰动；当前有效 best 模式为
`nominal_cpp_warm_start_eso_on_bw2p5`。由于本轮运行环境缺少 `pymavlink`，零扰动测试使用
已确认的 PX4 默认参数并跳过参数写入；这不改变 NMPC YAML，也不改变本轮 ESO/热启动验证结论。

## 2026-09-04 回归图缺口修复

复核圆形图后确认，旧图并非圆形参考公式错误，而是监督器 `trajectory.csv` 的 odometry 记录曾
出现大间隔：ESO+热启动正式圆形记录有约 `2.498 s` 缺口，仅热启动圆形记录有约 `2.938 s` 和
`2.681 s` 缺口。旧画图逻辑将缺口两端直接连线，因此把完整圆弧显示成了弦，造成“圆不满 360°”
的错误视觉结果。

已修改 `integration/plot_cpp_run.py`：自动检测超过 `0.1 s` 的时间缺口；存在缺口时，仅使用同一
案例同一轮 C++ `nmpc_flight.csv` 的完整采样绘制轨迹，并继续使用监督器汇总中的正式 Position
RMSE。图标题会标明实际可视化来源，缺口两端不会再被人为连接。已重画正式 combined 圆形图和
仅热启动圆形图；原始 CSV 和正式 RMSE 未被修改。

## 2026-09-04 C++ ESO 有无 × 三项扰动交叉验证（初步 2×2）

为确认 ESO 是否真正改善 C++ 版本的扰动跟踪，新增
`integration/run_cpp_eso_disturbance_cross_validation.py`。本轮固定当前 C++ 构建、solver
`1c2d851e`、原版 Q、热启动、`sample/reference/control=0.01 s`（100 Hz）、`N=30`、ESO
带宽 `2.5 rad/s`，按 `nominal/ESO关 → nominal/ESO开 → 三项扰动/ESO关 → 三项扰动/ESO开`
顺序执行。三项扰动为重心前偏 `0.02 m`、X 风 `0.5 m/s`、位置/速度随机游走 `0.002/0.005`。

与共享 Agent 的普通诊断流程不同，本轮每个案例都独立重启 MicroXRCE-DDS Agent、PX4/Gazebo、
C++ 节点和监督器，避免 DDS 会话跨案例污染；所有案例结束后进程均已清理，PX4 模型和世界文件
已恢复为无扰动状态。实际 C++ 节点 SHA-256 为
`fc42c5c2065fda60fefd5075c43ab845ba323677b9e24e0708cee061a55069d7`。

| 条件 | hover | point_1m | circle | figure8 | 结论 |
|---|---:|---:|---:|---:|---|
| nominal / ESO 关 | `0.0882` | `0.0747` | `0.1414` | `0.2131` | 四项 PASS，总和 `0.517450 m` |
| nominal / ESO 开 | `0.0218` | `0.0525` | `0.1444` | `0.1835` | 四项 PASS，总和 `0.402199 m` |
| 三扰动 / ESO 关 | 漂移保护 | 漂移保护 | `1.0495` | `1.2141` | 四项 FAIL；求解失败 `297` 次 |
| 三扰动 / ESO 开 | `0.4522` | 漂移保护 | `1.5735` | `1.3748` | 四项 FAIL；求解失败 `178` 次 |

无扰动时 ESO 使本轮总 RMSE 降低约 `22.2%`，但三项扰动全开时，ESO `2.5 rad/s` 没有使
四项达到验收阈值：hover 从漂移保护改善为完成但仍超阈值，point_1m 仍触发漂移保护，circle
和 figure8 的误差反而高于无 ESO。该结果不更新 best，也不支持直接提高 ESO 带宽；下一步应
先根据 `nmpc_timing.csv`、扰动估计和执行器响应检查 ESO 估计是否把噪声/延迟当成真实扰动。

正式交叉验证汇总：
`background/sitl_regression_cpp/cpp_eso_disturbance_cross_validation_20260904_162452/cross_validation_summary.md`；
机器报告：
`background/sitl_regression_cpp/cpp_eso_disturbance_cross_validation_20260904_162452/cross_validation_summary.json`；
16 项对比长图：
`background/sitl_regression_cpp/cpp_eso_disturbance_cross_validation_20260904_162452/cross_validation_trajectory_long.png`。

## 2026-09-04 C++ ESO 有无 × 单项/两两/三项全扰动完整交叉验证

根据前一轮只覆盖“无扰动/三项全开”的不足，本轮扩展为 8 种扰动组合 × ESO 开关 × 四项
轨迹，共 64 个独立案例。实际执行顺序为：无扰动、重心偏置、X 向风、里程计随机游走、
重心偏置+风、重心偏置+随机游走、风+随机游走、三项全开；每个组合均为 ESO 关后 ESO 开，
每种状态内部为 `hover → point_1m → circle → figure8`。固定 C++ 节点、solver `1c2d851e`、
原版 Q、热启动、`sample/control/reference=0.01 s`（100 Hz）、`N=30`、ESO 带宽 `2.5 rad/s`。
重心偏置为 `0.02 m`，X 向风为 `0.5 m/s`，里程计位置/速度随机游走为 `0.002/0.005`；
未启用的扰动均显式置零。

本轮修复并验证了两个测试基础设施问题：参数守护器不再把本地 GCS 的 system 255 无效心跳
当作 PX4；SITL 清理能识别本机 Gazebo 的不同包装命令，避免 `gz_bridge: Task already running`
污染下一案例。64 项均完成独立启动、运行和关闭，最终无残留 PX4、Gazebo、Agent 或 C++ 节点。
但有 1 项安全边界中止案例未能确认 `landed_disarmed`，须按失败记录处理，不能当作安全落地通过。

| 条件 | ESO 关 | ESO 开 | 位置 RMSE 顺序（hover/point/circle/figure8，m） |
|---|---:|---:|---|
| 无扰动 | 4/4 | 4/4 | 关 `0.0383/0.0683/0.1527/0.1872`；开 `0.0361/0.0621/0.1519/0.1703` |
| 单项：重心偏置 | 0/4 | 1/4 | 关 `inf/0.5985/1.0114/1.1098`；开 `1.4218/inf/0.8539/0.2178` |
| 单项：X 向风 | 3/4 | 4/4 | 关 `0.1181/0.2061/0.1806/0.2529`；开 `0.0757/0.0672/0.1513/0.1943` |
| 单项：里程计随机游走 | 4/4 | 4/4 | 关 `0.0612/0.0943/0.1774/0.2391`；开 `0.0552/0.0597/0.1615/0.1742` |
| 两项：重心偏置+风 | 0/4 | 0/4 | 关 `inf/inf/0.5746/1.2807`；开 `inf/inf/2.0713/3.6300` |
| 两项：重心偏置+随机游走 | 0/4 | 0/4 | 关 `inf/0.5511/1.0374/1.0060`；开 `inf/inf/0.9967/0.9545` |
| 两项：风+随机游走 | 3/4 | 4/4 | 关 `0.1041/0.2170/0.1946/0.3096`；开 `0.0571/0.0698/0.1694/0.1595` |
| 三项全开 | 0/4 | 0/4 | 关 `1.0895/0.3572/0.8971/0.6416`；开 `inf/inf/6.2461/0.5301` |

总计 `31/64` 通过。结论是：当前 ESO `2.5 rad/s` 对单项风、单项随机游走及风+随机游走
组合总体有帮助；重心偏置一旦出现，当前模型/控制配置仍会明显漂移，ESO 未解决重心偏置与
风或随机游走的组合；三项全开仍未达到四项验收标准。因此本轮不更新 nominal best，也不
据此直接提高 ESO 带宽。所有案例的详细 RMSE、最大误差、rx→pub 和 Acados solve
Median/P95/P99 见机器汇总。

本轮报告、机器 JSON、64 个案例目录和最终长图：
`background/sitl_regression_cpp/cpp_eso_disturbance_cross_validation_20260904_172210/`。
其中长图为 `cross_validation_trajectory_long.png`；本轮失败原因已逐项追加到
`docs/四项回归失败记录.md`。后续若修改 ESO、热启动、Q 矩阵或扰动模型，必须重新编译并
按本完整矩阵重跑，不能只凭某一项轨迹判断。

## 9. 当前文档分工

- `README.md`：安装、构建和常用命令。
- `docs/NMPC使用逻辑与条件.md`：接管、故障、恢复和参数变更条件。
- `docs/文件整理规则.md`：结果目录、保留和清理规则。
- `docs/NMPC时间同步工作记录.md`：时间戳、延迟和同步专项说明。
- `docs/四项回归操作流程.md`：改动检查、构建、服务时序、四项回归、长图和求解时间。
- `docs/RC遥控器注入测试规范.md`：AUX 接管、摇杆注入、RC 超时失效保护和单案例结果规范。
- `docs/四项回归失败记录.md`：每一轮失败案例、触发原因和诊断入口。
- 本文：当前阶段结论、基线和待办。
